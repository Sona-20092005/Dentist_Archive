spring:
  application:
    name: Dentist Archive
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT:5432}/${DB_NAME:dentist_archive_db}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

